# The nested type rosidl_typesupport_tickle_c_tests/msg/Branch.msg references from a *different*
# package - deliberately as minimal as tests/fixtures_own/Leaf.msg (tools/typesupport's own
# same-package nested fixture), just living in a genuinely separate ament package this time.
int32 value
